<%# console %>

<h2>Search Result</h2>

<% unless @users.empty? %>
  <% @users.each do |user| %>
    <p>
      <%= link_to user.name, user_path(user) %>
    </p>
  <% end %>
<% else %>
  <p>No matching user</p>
<% end %>

<p>
  <%= link_to 'user list', users_path %>
</p>